# Core dependencies for COMP2D dataset generation
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
tqdm>=4.66

# Graph representation (src/graph): raster->graph converter
scikit-image>=0.24
sknw>=0.15

# Optional: for development/testing
# jax>=0.6.2
# jaxlib>=0.6.2
# torch-geometric  # for to_pyg() Data objects; falls back to a tensor dict without it
